version: "3.7"

services:
  frontend-prod:
    build:
      context: .
      dockerfile:
        ./frontend/Dockerfile-prod
    ports:
      - 3000:80
    networks:
      - mynet

  backend-prod:
    ports:
      - 3001:3001
    build:
      context: .
      dockerfile:
        ./backend/Dockerfile-prod
    networks:
      - mynet

networks:
  mynet:
